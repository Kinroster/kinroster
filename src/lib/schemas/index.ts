// Barrel for the shared LLM output schemas. Add a new prompt's schema here as
// it is brought under eval coverage (Slice 3 registers the remaining five).

export * from '@/lib/schemas/shift-note';
export * from '@/lib/schemas/incident-classify';
